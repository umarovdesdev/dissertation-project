# §3.3.2 Ophthalmology-Specific Self-Supervised Pretraining of the CNN Backbone (V5 Arm)

> Draft generated per `prompts/writing-session-system-prompt.md` v6.0.0 · Brief: `briefs/3.3.2-brief.md` · Binding reference: INVARIANTS.md v6.0.0 · **NEW v6.0.0 section; lit-GAP — only #73 (`cheplygina-2018.md`) maps, as general framing.** Specification, not results: the ophthalmology-SSL (configs B/D) arm is not yet trained; no SSL performance is reported. Candidate-original design flagged; [UNSOURCED CLAIM] where applicable; no fabricated SSL citations. No figure (Resource IDs: —).

---

## PART 1: SECTION TEXT

Section 3.3.1 fixed the structural adaptation — the five-class softmax head — in common across all four configurations of Experiment 1. What distinguishes the configurations is not the head but the *initialization* of the backbone before fine-tuning, and this section specifies that initialization for the V5 arm. In the baseline arm (configurations A and C) the backbone is initialized from ImageNet weights, the standard cross-domain transfer from natural images. In the V5 arm (configurations B and D) the backbone is instead initialized from ophthalmology-specific self-supervised pretraining on an unlabeled retinal fundus corpus, performed on the same ResNet-50 or EfficientNet-B3 architecture used in the baseline arm (RESEARCH_ARCHITECTURE §4.2bis; DGL-6).

**The pretraining protocol.** The V5-arm initialization is produced by a CNN-compatible domain-adaptive self-supervised learning protocol drawn from the DINO, BYOL, SimCLR, and MoCo family of methods, with the specific protocol selected empirically and recorded once chosen. No diabetic-retinopathy labels are used at any point during this pretraining; the objective is purely representational — to learn, from unlabeled fundus images, features that capture the structure of retinal imagery: vascular topology, optic-disc and macular morphology, retinal texture, illumination variability, and imaging artifacts (RESEARCH_ARCHITECTURE §4.2bis). The resulting weights then initialize the same adapted network that the baseline arm initializes from ImageNet, after which both arms are fine-tuned identically on the labeled DR task (§3.3.3). Because the self-supervised pretraining is performed in-house rather than loaded from an external checkpoint, the encoder can be pretrained directly on the four-channel V5 tensor (RGB plus FOV mask), which removes the input-channel mismatch that a three-channel external checkpoint would create; the copy-and-mean-initialization of the fourth channel described in §3.2.1 remains available as a fallback (AOQ-2 simplified, INVARIANTS v6.0.0 §X).

**Why an in-domain, CNN-native initialization.** The design rationale for this choice is to obtain an in-domain initialization without confounding the experiment with an architecture change. A retinal foundation model such as RETFound is published as a ViT-Large vision transformer; initializing the V5 arm from it would change both the architecture and the initialization relative to the baseline arm, so that any observed difference would conflate the preprocessing contribution with an architecture difference. A CNN-native self-supervised initialization changes only the initialization stage and preserves the convolutional backbone across both arms (the reasoning recorded in INVARIANTS v6.0.0 §X, which resolves the earlier open question on backbone architecture in favour of CNN-compatible SSL, restores the 2×2 architecture symmetry across arms, and renders the external foundation-model license question moot). The in-domain initialization is thus the dissertation's own methodological response to a confound, not an adoption of an existing foundation model.

**The composite independent variable.** The consequence of this design for the interpretation of Experiment 1 is binding and must be stated before any result is reported. The V5 arm differs from the baseline arm along two axes simultaneously — the preprocessing axis (the full V5 pipeline versus stretch-resize with ImageNet normalization) and the pretraining axis (ophthalmology-specific self-supervised initialization versus ImageNet initialization). The independent variable of Experiment 1 is therefore the composite factor *(preprocessing × pretraining source)*, and any performance difference between the arms reflects the joint contribution of both axes. It follows that the H-1 effect may not be attributed to the preprocessing pipeline alone, nor to the pretraining source alone: claims of the form "the preprocessing pipeline produces the observed improvement" or "preprocessing dominates" are not admissible (CFC-2.8). The only admissible form of the H-1 claim is at the level of the integrated configuration — that the integrated V5-plus-ophthalmology-SSL configuration outperforms the baseline-plus-ImageNet configuration on a given metric by a given margin. Decomposition of the composite effect into separate preprocessing and pretraining contributions would require an additional factorial that is explicitly outside the scope of this dissertation and is named as future work.

**Literature status and honesty conditions.** Two limitations of this specification are stated plainly. First, it rests on a genuine gap in the assembled literature. The corpus contains a general survey of self-supervised, multiple-instance, and transfer learning under limited labels in medical imaging (Cheplygina et al., 2018), which establishes that label-efficient representation learning is an active and credible direction in medical image analysis; it does not, however, contain primary sources for in-domain self-supervised pretraining on retinal fundus imagery specifically — the DINO, BYOL, SimCLR, and MoCo primary methods, and any ophthalmology-specific self-supervised pretraining studies, remain to be acquired [UNSOURCED CLAIM: the expectation that in-domain self-supervised pretraining yields fundus-relevant representations is, at this stage, the dissertation's own methodological hypothesis, supported only by the general survey #73 and not by a retinal-SSL primary source]. The in-domain self-supervised initialization is, accordingly, presented as a candidate methodological contribution whose efficacy is to be established empirically, not as a result inherited from the literature. Second, this section specifies a design, not an outcome: the ophthalmology-self-supervised arm (configurations B and D) is a specification to be executed, and no self-supervised pretraining results are reported here or claimed elsewhere in this dissertation in advance of that execution.

With the V5-arm initialization specified and bounded, §3.3.3 specifies the two-stage fine-tuning schedule by which the adapted, initialized network — under either initialization — is trained on the labeled DR task, and §3.3.4 specifies the loss it is trained against.

---

## PART 3: COMPLIANCE CHECKLIST

**CFC-2.8 (composite IV — no single-factor attribution) [CENTRAL]** — ✅ Satisfied. *"The independent variable of Experiment 1 is therefore the composite factor (preprocessing × pretraining source) … the H-1 effect may not be attributed to the preprocessing pipeline alone, nor to the pretraining source alone: claims of the form 'the preprocessing pipeline produces the observed improvement' or 'preprocessing dominates' are not admissible (CFC-2.8). The only admissible form … is at the level of the integrated configuration"* (composite-IV ¶). Permissible framing stated verbatim; decomposition named as out-of-scope future work.

**DGL-6 (pretraining regimes; no guaranteed transfer)** — ✅ Satisfied. *"In the baseline arm … ImageNet weights … In the V5 arm … ophthalmology-specific self-supervised pretraining on an unlabeled retinal fundus corpus … on the same ResNet-50 or EfficientNet-B3 architecture"* (¶1); efficacy "to be established empirically" (honesty ¶). No DR labels in pretraining stated.

**CFC-2.1 (no universal generalization)** — ✅ Satisfied (absent). SSL efficacy is "a candidate methodological contribution whose efficacy is to be established empirically," not a universal claim. Closest-checked: honesty ¶.

**CFC-2.2 (no superiority over RETFound/foundation models)** — ✅ Satisfied (absent). RETFound discussed only as the retired design and confound rationale: *"initializing the V5 arm from it would change both the architecture and the initialization"* (why-in-domain ¶); no superiority claim.

**CFC-2.4 (no clinical-grade claim)** — ✅ Satisfied (absent). No clinical-validation register; closest-checked is the representation-objective list (technical, not clinical).

**CFC-2.7 (no retroactive recharacterization)** — ✅ Satisfied (no self-pub recharacterized; none cited as SSL evidence).

**NC-6 / SB-3.1 (no optimality)** — ✅ Satisfied. SSL protocol "selected empirically," not claimed optimal; closest-checked: protocol ¶.

**SIR-1 (non-amplification; lit-GAP) [CENTRAL]** — ✅ Satisfied. *"it does not, however, contain primary sources for in-domain self-supervised pretraining on retinal fundus imagery specifically"* (honesty ¶); #73 used only for the general direction, with one explicit `[UNSOURCED CLAIM]` marking the candidate hypothesis. No fabricated SSL citation.

**Untrained-status honesty** — ✅ Satisfied. *"this section specifies a design, not an outcome … no self-supervised pretraining results are reported here or claimed elsewhere … in advance of that execution"* (honesty ¶).

**SB-1.4 / IT-1 (fundus only)** — ✅ Satisfied (fundus corpus throughout; no other modality). Closest-checked: protocol ¶.

**Paradigm positioning (CFC-2.9 / SIR-9)** — ⚪ Not applicable (no Gulshan attribution; CNN-native rationale is own design).

**EH-3 / EH-4** — ⚪ Not applicable here; EH-3/EH-4 evaluated in §4.2 (and the composite-IV constraint shapes how the EH-3 verdict is phrased there).

---

### Word count

PART 1 body: ~1,000 words by manual count (within the 900–1,100 target; mid-band).

### Deferred-asset log

None (no asset). **Literature-acquisition flag:** primary in-domain retinal SSL sources (DINO/BYOL/SimCLR/MoCo on fundus; ophthalmology-SSL) to be acquired — also blocks the deferred §2.3.3. Recorded for the corpus-completion queue.
