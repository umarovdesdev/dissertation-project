# CONTINUITY NOTE — §3.3.2 Ophthalmology-Specific Self-Supervised Pretraining of the CNN Backbone (V5 Arm)

**Version:** 6.0.0 · PART 2 of the §3.3.2 session · Carry into §3.3.3 (step c). **NEW v6.0.0 section.**

---

**Key concepts established:**
- **Initialization distinguishes the arms:** baseline (A/C) = ImageNet; V5 (B/D) = **ophthalmology-specific self-supervised pretraining** of the SAME backbone on an unlabeled retinal fundus corpus (no DR labels; representation objective over vascular topology / OD-macular morphology / texture / illumination / artifacts).
- **Protocol:** CNN-compatible SSL from the **DINO/BYOL/SimCLR/MoCo** family, selected empirically and recorded once chosen.
- **CNN-native rationale:** avoids the architecture confound of a ViT foundation model (RETFound); resolves AOQ-1→(b), AOQ-3 (license moot), AOQ-4 (2×2 symmetry restored); AOQ-2 simplified (4-channel SSL pretraining; copy-and-mean-init fallback).
- **CFC-2.8 composite IV (binding):** V5 arm differs along BOTH preprocessing and pretraining axes; IV = (preprocessing × pretraining source); **no single-factor attribution** (not preprocessing-alone, not pretraining-alone); only integrated-configuration H-1 claims admissible; decomposition = out-of-scope future ablation.
- **lit-GAP + honesty:** #73 (Cheplygina) general survey only; primary in-domain retinal SSL sources ABSENT (candidate methodological hypothesis, one [UNSOURCED CLAIM] marker); **specification, not result** — SSL B/D arm untrained.

**Terms carried:** ophthalmology-specific SSL; in-domain initialization; CNN-native; composite independent variable (preprocessing × pretraining); DINO/BYOL/SimCLR/MoCo; 4-channel SSL pretraining.

**Argument thread:** The four configurations are now fully distinguished — common adaptation (§3.3.1), arm-specific initialization (§3.3.2). The thread proceeds to how the adapted, initialized network is *trained*: §3.3.3 (fine-tuning schedule) and §3.3.4 (loss).

**Final topic:** Closing sentence hands off to §3.3.3 (two-stage fine-tuning) and §3.3.4 (loss).

**Setup for next section (§3.3.3 — Two-Stage Fine-Tuning Protocol Design):** Word band 600–800. Open by taking both initializations (ImageNet baseline; ophthalmology-SSL V5) as given and specify the fine-tuning *schedule* applied identically: the two-stage (frozen-base head-training → progressive unfreezing) protocol. Cards #19/#21 (one thread, SIR-4/5/7): prior frozen vs fine-tuned record (F1 0.62→0.74; precision 65%→75%) — **H-3 was DROPPED in V3**, so the two strategies are a training *method*, not a tested hypothesis (state this). Bindings SIR-4/5; SIR-7 (B0 not generalized); training-method-only framing. Applied identically across arms/backbones (factorial cleanliness, EH-4).

**Unresolved threads / flags (carry forward):**
- **CFC-2.8 composite IV** — carry into §4.2 (results-time attribution).
- **lit-GAP** primary SSL sources (blocks §2.3.3; corpus-completion queue); intentional [UNSOURCED CLAIM] kept.
- **SSL B/D arm untrained** (project memory) — no SSL results anywhere until run.
- **SIR-7**; **[VERIFY] Stage-5 polar CLAHE**; **#23 integrity flag** — non-blocking.
