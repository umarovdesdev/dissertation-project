# CONTINUITY NOTE — §3.1.3 Augmentation Strategy for Class Imbalance Mitigation

**Version:** 6.0.0 · PART 2 of the §3.1.3 session · Carry into §3.1.4 (step c).

---

**Key concepts established:**
- Stage 6 is **train-only**; inference is deterministic; no augmented image enters any evaluation partition (leakage control, §9.1 / §3.4.2).
- Three perturbation families: **geometric** (unified affine — rotation with Stage-1-uncertainty-adaptive dispersion, zoom [0.9,1.1], shear/stretch; isotropy-preserving); **photometric** (brightness/contrast jitter, coordinated with Stage-5 stochastic CLAHE); **chromatic** (Krizhevsky-style PCA colour jitter, σ=0.1).
- **PCA colour basis is the real EyePACS basis (RES-PCA):** eigvals ≈ [20.42, 128.06, 2374.04] → one dominant chromatic axis (~18× the next, ~116× the least); jitter perturbs predominantly along EyePACS's dominant colour-variation direction. Method ← #65; basis ← own estimation (DGL-1 EyePACS-bound).
- **Dual role:** regularization (§2.2.3) + **one of two class-imbalance levers** (SC-1.4); the principal imbalance mechanism is the inverse-frequency-weighted focal loss (§3.3.4). Neither claimed to resolve imbalance (NC-17; SB-2.1).

**Terms carried:** unified affine; adaptive rotation dispersion; zoom/shear/stretch; brightness/contrast jitter; PCA color jitter (σ=0.1); principal components / eigenvalues; dominant chromatic axis; train-only augmentation; leakage control.

**Argument thread:** With Stage 6 specified, the eight-stage train-time pipeline is complete (Stage 7 specified in §3.1.1). The thread now leaves the canonical pipeline to address the *entry conditions* for real clinical data: §3.1.4 specifies the external-image ingestion protocol that precedes Stage 0.

**Final topic:** Closing sentence hands off to §3.1.4 — clinical images arriving "heterogeneous, sometimes degraded, sometimes mislabeled," outside the controlled conditions the eight stages assume.

**Setup for next section (§3.1.4 — External Image Ingestion Protocol):** This is a **lit-GAP / candidate methodological contribution** (no external literature; Coverage Matrix). Mark candidate-original analysis as `[UNSOURCED CLAIM]` where appropriate; do NOT fabricate citations. Bind to **NC-15** (ingestion protocol validated only for Kazakh clinical data, not arbitrary sources). Frame as a design specification (preceding Stage 0) for ingesting heterogeneous clinical fundus images (format normalization, quality gating, laterality/label sanity checks) — a contribution, not a validated general-purpose cleaner. GDPR/handling concerns are §6.4.1, not here. Connect to §3.1.1 (Stage 0 entry) and the Clinical dataset (RESEARCH_ARCHITECTURE §2.1.8: 60 images, Kazakh medical center).

**Unresolved threads / flags (carry forward):**
- **[VERIFY] Stage-5 governance/implementation divergence** (polar CLAHE) — governance pass before §4.3.
- **#23 card integrity flag** — non-blocking.
- Class-imbalance second lever (weighted focal loss) → §3.3.4; class distribution → §4.1.2; augmentation-level marginal value → §4.3 (Exp 2).
- **SIR-7** (EfficientNetB0 not generalizable) — continue honoring in §3.2.2/§3.3.
