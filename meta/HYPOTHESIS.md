If contrast-adaptive preprocessing of fundus images (including upgraded CLAHE with threshold control, resizing, normalization, and augmentation) is integrated into a CNN-based classification pipeline while keeping the classifier architecture fixed, then the diagnostic performance (measured by accuracy, precision, recall, F1-score, and ROC-AUC) for multi-stage diabetic retinopathy classification will be statistically significantly higher than that of an equivalent CNN model trained without such preprocessing under the same computational conditions.