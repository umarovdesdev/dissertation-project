# Set after the backend is deployed (e.g. HuggingFace Space):
#   REACT_APP_API_URL=https://yesmukhamedov-dr-demo-config-d.hf.space
# Empty → frontend runs simulator-only (no backend calls).
REACT_APP_API_URL=
